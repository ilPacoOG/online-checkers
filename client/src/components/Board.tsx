import { FC } from 'react';

interface BoardProps {
  // Add props as needed
}

export const Board: FC<BoardProps> = () => {
  return (
    <div className="board">
      {/* Board implementation */}
    </div>
  );
};